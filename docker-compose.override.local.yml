---
version: '3.4'

services:
  backend:
    volumes:
      - ./backend/src/xfd_django:/app
      - ./backend/external:/app/external
      - ./backend/pe:/app/pe

  pe-worker:
    volumes:
      - ./backend/pe:/app
